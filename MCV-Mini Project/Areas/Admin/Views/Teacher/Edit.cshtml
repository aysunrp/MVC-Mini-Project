@model TeacherEditVM
@{
    ViewData["Title"] = "Müəllim Redaktə";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="admin-card">
            <div class="admin-card-header">
                <h5><i class="bi bi-pencil me-2 text-warning"></i>Müəllim Redaktə — #@Model.Id</h5>
                <a asp-action="Index" class="btn btn-sm btn-outline-secondary">
                    <i class="bi bi-arrow-left me-1"></i>Geri
                </a>
            </div>
            <div class="p-4">
                <form asp-action="Edit" method="post" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()
                    <input asp-for="Id" type="hidden" />

                    <div class="mb-3">
                        <label asp-for="FullName" class="form-label fw-semibold">Ad Soyad</label>
                        <input asp-for="FullName" class="form-control" />
                        <span asp-validation-for="FullName" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-semibold">Şəkil</label>
                        @if (!string.IsNullOrEmpty(Model.CurrentImage))
                        {
                            <div class="mb-2">
                                <img src="@FileHelper.GetImageUrl(Model.CurrentImage)" alt="current"
                                     style="width:100%;max-height:180px;object-fit:cover;border-radius:8px;border:1px solid #e5e7eb" />
                            </div>
                        }
                        <div class="drop-zone">
                            <input type="file" asp-for="ImageFile" accept="image/*" />
                            <i class="bi bi-cloud-arrow-up"></i>
                            <div class="dz-label">Yeni şəkil üçün <span>seçin</span></div>
                        </div>
                        <img class="img-preview mt-2" alt="preview" />
                    </div>

                    <div class="mb-4">
                        <label asp-for="PositionId" class="form-label fw-semibold">Vəzifə</label>
                        <select asp-for="PositionId" asp-items="ViewBag.Positions" class="form-select">
                            <option value="">— Vəzifə seçin —</option>
                        </select>
                        <span asp-validation-for="PositionId" class="text-danger small"></span>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-warning">
                            <i class="bi bi-check-lg me-1"></i>Yenilə
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary">Ləğv et</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
