@model EventCreateVM
@{
    ViewData["Title"] = "Yeni Tədbir";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="admin-card">
            <div class="admin-card-header">
                <h5><i class="bi bi-plus-circle me-2 text-warning"></i>Yeni Tədbir</h5>
                <a asp-action="Index" class="btn btn-sm btn-outline-secondary">
                    <i class="bi bi-arrow-left me-1"></i>Geri
                </a>
            </div>
            <div class="p-4">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()

                    <div class="row mb-3">
                        <div class="col-md-3">
                            <label asp-for="DateDay" class="form-label fw-semibold">Gün</label>
                            <input asp-for="DateDay" class="form-control" placeholder="15" />
                            <span asp-validation-for="DateDay" class="text-danger small"></span>
                        </div>
                        <div class="col-md-9">
                            <label asp-for="DateMonth" class="form-label fw-semibold">Ay</label>
                            <input asp-for="DateMonth" class="form-control" placeholder="Yanvar" />
                            <span asp-validation-for="DateMonth" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Title" class="form-label fw-semibold">Başlıq</label>
                        <input asp-for="Title" class="form-control" placeholder="Tədbir adı" />
                        <span asp-validation-for="Title" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Location" class="form-label fw-semibold">Yer</label>
                        <input asp-for="Location" class="form-control" placeholder="Bakı, Azərbaycan" />
                        <span asp-validation-for="Location" class="text-danger small"></span>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-warning">
                            <i class="bi bi-check-lg me-1"></i>Əlavə et
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary">Ləğv et</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
