@model IEnumerable<SettingListVM>
@{ ViewData["Title"] = "Settings"; }

<div class="ac">
    <div class="ac-head">
        <h5><i class="bi bi-gear me-2" style="color:#ff8a00"></i>Settings</h5>
        <a asp-action="Create" class="btn btn-sm btn-orange"><i class="bi bi-plus-lg me-1"></i>Add Setting</a>
    </div>
    <div class="table-responsive">
        <table class="table at mb-0">
            <thead><tr><th>#</th><th>Key</th><th>Value</th><th class="text-end">Actions</th></tr></thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr><td colspan="4" class="text-center text-muted py-4">No settings found.</td></tr>
                }
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-muted small align-middle">@item.Id</td>
                        <td class="align-middle"><code>@item.Key</code></td>
                        <td class="align-middle">
                            <span class="text-truncate d-inline-block" style="max-width:260px">@item.Value</span>
                        </td>
                        <td class="text-end align-middle">
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary me-1"><i class="bi bi-eye"></i></a>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning me-1"><i class="bi bi-pencil"></i></a>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline"
                                  onsubmit="return confirm('Delete this setting?')">
                                @Html.AntiForgeryToken()
                                <button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
